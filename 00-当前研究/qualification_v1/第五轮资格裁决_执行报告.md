# 第五轮资格裁决执行报告

**轮次：AIDD Research Qualification v1｜日期：2026-09-13｜入口：`00-当前研究/qualification_v1/`｜git：f0f245b**

## 1. 执行摘要

三库（aidd 主库 / mash-aidd-workbench / hepato-gnn-screening）统一为同一套科研事实口径；前四轮 38 项资产完成 A/B/C/D 四类审计；assay-level provenance 审计 PASS（51 assay、0 缺失 lineage）；RERUN_DECISION 裁决 14 项中**仅 GNN benchmark 进入复算**并已实跑——**GNN 未证明优于经典 baseline**（同折 10-seed：GCN R²中位 0.078、GINE −0.040 vs Ridge 0.732；同折断言逐位复现资格轮数字）。G0/G1/G2 PASS、G3/G4 FAIL（阴性冻结）、`candidate_release=false` 不变。Workbench 新增只读 Research State 页并完成全部接线测试。本轮全部 QC PASS。

## 2. 三库状态

| 仓库 | 位置 | 角色（冻结） | git |
|---|---|---|---|
| aidd 主库 | `D:\zcode-workspace\aidd-repo-work` | 唯一正式科研结论入口 | def8433 归档此前 untracked 全部轮次 + f0f245b 本轮 |
| mash-aidd-workbench | `D:\aidd destoop`（v0.5.2） | 执行/审计平台；非第二条候选发布链 | 619125a 仅新增 17_Research_State 页+测试；v0.5.2 发布线既有未提交修改未触碰 |
| hepato-gnn-screening | `D:\zcode-workspace\hepato-gnn-screening` | GNN 方法学习+历史核验+组员工作台 | 本轮只读审计，无改动 |

## 3. 前四轮资产分类

38 项（ASSET_AUDIT.csv，路径存在性机检通过）：**VERIFIED 22 / REQUALIFY 2 / HISTORICAL 9 / REJECTED 5**。REQUALIFY 两项：GNN 库 100 条结构修订（待组员人工身份审核）、FXR 历史主稿证据（FXR 支线重启才需重做）。REJECTED 五项：docking 排序用途（WT gate ROC 0.472）、"唯一获批"表述、MASH v2 聚类/校准/靶点确证表述、AI 生成两周收尾批次、旧标签体系。历史材料全部保留，仅降级不删除。

## 4. 数据资格

G1 **PASS（限定 canonical）**：FASN_activity_master 1,652 条/51 assay 记录级 lineage（assay/document/DOI/PMID/物种/单位转换方法）完整；本轮审计 0 缺失、canonical 96 分子全覆盖、外部 0 与训练重叠、mirror assay 已排除。"通用 FASN 数据资格"未申请（Gate T1 前不存在多 assay 训练集）。

## 5. 模型资格

G2 **PASS（canonical 内部证据齐备）**：scaffold split 10 seeds + mean/std/median/min/max 报告 + baseline 对比 + Y-scrambling ×20 + leakage audit 全部成立（资格轮实跑，本轮引用+独立复算复核：Ridge 中位 R² 0.7320079614617288 与冻结值逐位一致）。本轮补齐 GNN 维度（见第 6 节）。

## 6. GNN 结论

**GNN 当前未证明优于经典 baseline**（E07/C10，本轮首次实证）。与资格轮完全同折（断言 PASS）：Ridge 0.732 > XGB 0.594 >> RF 0.217 > GCN 0.078 > GINE −0.040（R²中位）。Y-scrambling：GCN 0/20 exceeded（微弱真信号）；GINE 13/20 置换反超（seed0 无信号）。MC-dropout UQ 无效（方差-|误差| rho≈0）。方法学习审计：旧 numpy GCN 是真实现（传播/手推反向/Adam 均真实）但特征与 split 不达标（train/test 实测共享 2 骨架、未知元素回退碳槽位、无键特征、标签 REJECTED）；新 PyG 实现（33 维原子特征+6 维键特征 GINE）清单全过。GNN 门控**维持关闭**，首次有实证依据。

## 7. 外部验证

G3 **FAIL（阴性冻结）**：资格轮结果不变（105 分子/3 文献 assay：Ridge R² 0.180、分 assay 全负、conformal 覆盖 0.629）。本轮新增 GNN 外部评估：全负（GCN −0.237 / GINE −0.150）且无 ADin 排序优势（Ridge ADin rho 0.8796/n=14 与推进轮 E1_ADin 逐位复现）。禁止为翻盘改 split/重跑。

## 8. AD/UQ

AD 阈值 0.8116（LOO 5%）正确隔离 91/105 OOD 与 14 ADin；conformal 内部有效/外部失效维持原结论；GNN MC-dropout 方差不构成可用不确定性指标（如实废弃）。

## 9. Docking

G4 **FAIL（排序用途）/结构假说用途 HOLD**：WT TRβ ROC-AUC 0.472 维持终审；本轮零 docking 计算（RERUN_DECISION R10）；协议记录要求（receptor/PDB/chain/CCD/grid/exhaustiveness/seed/版本/RMSD）已在门控文档重申。

## 10. Literature Evidence

MASTER_EVIDENCE_MATRIX E01–E21：claim → evidence → source → confidence 全链落盘，文献条目仅引用主库已留痕 PMID/DOI（Moracin N 五篇核心：34281775/40972263/32477104/31610178/36495698 + 先例 39922149 + 来源定量 31008101 + denifanstat 39396529 + NaV off-target 31262454 + 桥接 21605850/15943584）。Workbench Literature Hub（15/16 页）基础设施保留，正式证据以主库矩阵为准（17 页已接入展示）。

## 11. Workbench

只读接线（不重设计）：新增 `pages/17_Research_State.py`（RESEARCH_STATE/门控/资产台账/证据矩阵/GNN 基准/QC/OPEN_ISSUES 镜像；`AIDD_QUALIFICATION_ROOT` 可覆盖；缺失即 BLOCKED 不伪造）+ `tests/test_research_state_page.py`。AppTest 真实渲染 0 异常。

## 12. 当前 Candidate Release 状态

**`candidate_release = false`（维持）**。G3/G4 未过，按放行政策禁止任何正式候选发布。池内状态：Moracin N=GO（实验启动资格，非候选放行，等物料）；其余历史/降级/对照。

## 13. 实际运行的测试

| 测试 | 结果 |
|---|---|
| audit_assay_provenance.py | PASS（51 assay/0 缺失/0 外部重叠） |
| run_gnn_clean_benchmark.py | COMPLETED 275.5s；同折断言 PASS；scrambling 汇总修正后经 --summary-only 重建 |
| run_qualification_qc.py（本轮） | **PASS 0 issues**（3 轮修复迭代后） |
| 资格轮 QC 回归复跑 | PASS（qc_auto 0 issues） |
| 推进轮 QC 回归复跑 | PASS（qc_auto 0 issues） |
| workbench pytest 全套 | **113 passed**；1 deselected（conplex 真实模型：Windows 页面文件不足 os error 1455，预存环境限制非回归，已登记） |
| Streamlit headless | / 与 /17_Research_State 均 200 |
| 新页 AppTest 渲染 | 0 异常（10 markdown/3 dataframe） |
| GNN 库导入冒烟 | models.{gnn,dataset,baseline} OK |

## 14. 修复过的问题

1. PyG 2.5.3：GINConv 不接受 edge_dim → 改 GINEConv，且 MLP 输入维=节点维（edge 特征由内部 encoder 对齐）；
2. benchmark 汇总聚合键名错误（gcn vs gnn_gcn）产生 NaN → 修复 + `--summary-only` 从 CSV 重建（模型未重训，JSON 留痕）；
3. QC 路径解析器三轮迭代：先去括注再分割、上下文仅继承裸名、尾斜杠归一；
4. ASSET_AUDIT C05 行缺仓库列导致整行错位、E06 confidence 带修饰、D02/C08/A21/D04 路径不可解析 → 全部修正；
5. 违禁表述扫描误报（否定语境/政策行/自身产物）→ 豁免规则。

## 15. 尚未解决问题

外部阻塞：O1 Moracin N 物料未到货；O2 导师签核 not_recorded；O3 CNIPA/专利/知网检索边界。待决计算：O5 **Gate T1**（多 assay 对齐数据集，下一轮主计算）；O6 GNN 门控连同 T1 一起复审；O7 组员结构人工审核；L2 GNN 结论限定当前数据规模。

## 16. 下一阶段推荐路线

1. **Gate T1（FASN，最高优先计算）**：从 master 51 assay 组装 ≥15 共享分子/assay 对 → assay 间变换 → 归一化训练集重跑分层验证；失败 → FASN 降级方法学线；
2. **Moracin N**：导师批准 + GlpBio(¥520)/MCE 下单 → 到货即执行 M0 六项（启动包已冻结）；
3. GNN 门控复审（连同 T1 数据；当前实证不支持启用）；
4. 组员：VERIFY_TASKS 结构审核（B01）继续。

## 17. Git 修改摘要

- **aidd 主库**：`def8433` 归档 2026-09-09~09-13 全部此前 untracked 轮次（筛选干跑/Phase2/候选决策/第五/六/七轮/最终战略/资格验证/推进轮 + validation）；`f0f245b` qualification_v1 全部交付（含 README/研究状态.json 同步）。工作树 clean。
- **workbench**：`619125a` 新增 pages/17_Research_State.py + tests/test_research_state_page.py（仅此两文件；v0.5.2 发布线既有修改未触碰）。
- **hepato-gnn-screening**：无改动（只读审计）。

## 18. 状态分层（按指令要求）

- **已验证**：第 4/5/6/7/8/9/10/11/13 节所列全部数字（脚本+日志+QC 留痕）；
- **软件通过**：Workbench 平台功能、GNN 库代码资产（不等于科研结论）；
- **数据待人工核验**：GNN 库 100 条结构修订（B01，组员任务）；
- **科学证据不足**：FASN 跨 assay 泛化（T1 未做）、GNN 增益（阴性实证）、Moracin N 全部生物学判读（未执行）；
- **外部阻塞**：O1 物料、O2 导师签核、O3 检索边界；
- **暂停路线**：THRβ 主动投入、FXR 支线、ACC、旧 GNN 筛选线、docking 排序（永久）。

# 未完成项与风险

## P0/P1

- O1 Moracin N 物料仍未到货；M0/B0/1/2/3 生物学验证未执行；
- O2 导师签核未记录；
- Gate T1 多 assay 对齐尚未执行；FASN 仍是 canonical qualified local，不是一般 FASN 泛化模型；
- GNN enable gate closed；不得从旧库直接产生 NP54 FASN ranking；
- GNN 100 条结构修订仍需人工身份核验，不能自动升级 VERIFIED。

## 资产风险

- 旧聚合标签与旧 split 可以复现，但不一定具有完整 assay lineage；
- 旧 FXR 模型是内部 scaffold test，不是 external validation；
- 2J4A/N331S 文件和旧 docking ranking 仍容易被误读，必须依赖 registry 和状态说明；
- Workbench 当前有 v0.5.2 发布线既有未提交修改，本轮不应覆盖或混入。

## 本轮没有做的事

- 没有删除任何历史数据、模型、日志或结构；
- 没有重跑全部历史模型或 docking；
- 没有擅自改 GNN 仓库名；
- 没有把 GNN benchmark 阴性结果改写成模型失败以外的故事，也没有为翻盘调参。
